name: Code Review

permissions:
  contents: read
  pull-requests: write

on: [issue_comment]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: multiversx/mx-assets-checker@init
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}


